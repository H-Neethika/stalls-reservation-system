package com.exhibition.exhibition_service.enums;

/**
 * Booking availability states stored directly as strings in the stall row.
 */
public enum BookingStatus {
    AVAILABLE,
    RESERVED,
    UNAVAILABLE
}

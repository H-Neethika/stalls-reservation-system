server:
  port: ${EUREKA_SERVER_PORT:9001}

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:9001/eureka/}
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
